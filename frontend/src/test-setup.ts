/**
 * Vitest global test setup.
 *
 * Imports @testing-library/jest-dom so custom matchers like
 * `toBeInTheDocument()` and `toHaveAttribute()` are available in every test.
 */
import '@testing-library/jest-dom';
