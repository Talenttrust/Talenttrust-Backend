/**
 * @deprecated Import `validateRequest`, `validateParams`, `validateQuery` directly
 * from `./validate.middleware` instead. This module is retained as a thin re-export
 * shim for backward compatibility and will be removed in a future version.
 */
export {
  validateRequest,
  validateParams,
  validateQuery,
} from './validate.middleware';
