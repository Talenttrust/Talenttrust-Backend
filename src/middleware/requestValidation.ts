/**
 * @deprecated Import `validateSchema` directly from `./validate.middleware` instead.
 * This module is retained as a thin re-export shim for backward compatibility and
 * will be removed in a future version.
 */
export { validateSchema } from './validate.middleware';
export type { ValidationErrorResponse } from './validate.middleware';
